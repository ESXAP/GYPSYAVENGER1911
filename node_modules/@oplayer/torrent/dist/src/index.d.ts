import type { Player, PlayerPlugin, Source } from '@oplayer/core';
export type PluginOptions = {
    config?: Record<string, any>;
    matcher?: (src: Source) => boolean;
};
declare class TorrentPlugin implements PlayerPlugin {
    options: PluginOptions;
    key: string;
    name: string;
    version: any;
    static defaultMatcher: PluginOptions['matcher'];
    player: Player;
    instance: any;
    prePreload?: HTMLMediaElement['preload'];
    constructor(options: PluginOptions);
    apply(player: Player): void;
    load({ $video }: Player, source: Source): Promise<false | this>;
    unload(): Promise<void>;
    destroy(): Promise<void>;
}
export default function create(options?: PluginOptions): TorrentPlugin;
export {};
//# sourceMappingURL=index.d.ts.map